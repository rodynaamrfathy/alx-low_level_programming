#include "main.h"

/**
 * _abs - a function that computes the absolute value of an integer.
 * @n: number
 *
 * Return: returns the number
 */
int _abs(int n)
{
	int r = (n < 0)? -n : n;

	return (r);
}
